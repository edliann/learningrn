export type VehicleType = {
  id: string;
  name: string;
  capacityLabel: string;
  recommendedQuantityLabel: string;
  baseFee: number;
  active: boolean;
  createdAt: string;
  updatedAt: string;
};
